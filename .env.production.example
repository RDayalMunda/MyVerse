# Production API URL baked into release APK builds.
# Copy to .env.production before running npm run build:apk
#
# EXPO_PUBLIC_* values are embedded at build time — not read at runtime from .env on device.

EXPO_PUBLIC_API_URL=https://myverse.redvalky.in/api/v1
